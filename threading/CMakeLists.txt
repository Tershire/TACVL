cmake_minimum_required(VERSION 3.22)
project(Threading)

# find_package(OpenCV 4 REQUIRED)

add_executable(basic_threading basic_threading.cpp)

# target_link_libraries(basic_threading ${OpenCV_LIBS})
